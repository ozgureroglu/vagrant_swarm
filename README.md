### Vagrant SWARM installation

Howto setup a virtualized 3 node swarm using vagrant.

*Note:* Requires you to install nfs-kernel-server to your linux box to enable nfs share to share the swarm join token
.
